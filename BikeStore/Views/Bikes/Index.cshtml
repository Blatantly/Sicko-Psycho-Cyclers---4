@model IEnumerable<BikeStore.Models.ProductCategory>

@{
    ViewData["Title"] = "Bikes";
}

<h1>Browse our bike models</h1>

<table class="table">
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.ActionLink(item.Name, item.Name.Substring(0, item.Name.IndexOf(" ")))
            </td>
        </tr>
}
    </tbody>
</table>
